services:
  backend:
    build:
      context: ./server
    ports:
      - "5000:5000"
    environment:
      MONGODB_URI: ${MONGODB_URI}

  frontend:
    build:
      context: ./client
    ports:
      - "4173:4173"